### React এ Object State আপডেট করার পদ্ধতি

**রিঅ্যাক্টে অবজেক্ট আপডেট: জাভাস্ক্রিপ্টের সাথে পার্থক্য:**

React-এ state এ আপনি যেকোন ধরনের JavaScript ভ্যালু রাখতে পারবেন, এমনকি object-ও। চলুন দেখি JavaScript-এ আমরা কীভাবে object আপডেট করি:

```jsx
javascript
Copy code
const person = {
  name: "rafsan",
  age: 25
};

person.age = 31;

```

সাধারণত JavaScript-এ আমরা এইভাবে object আপডেট করি। কিন্তু React-এ আপনি এইভাবে আপডেট করতে পারবেন না। React-এ state আপডেট করার জন্য আপনাকে একটি নতুন object তৈরি করতে হবে অথবা বিদ্যমান object-এর একটি কপি তৈরি করতে হবে।

### রিঅ্যাক্ট স্টেট আপডেট: Immutable অবজেক্ট ব্যবহার

JavaScript-এ object সাধারণত mutable (সরাসরি পরিবর্তন করা যায়) হয়। কিন্তু React-এ আপনাকে object-কে immutable (সরাসরি পরিবর্তন করা যায় না) হিসেবে বিবেচনা করতে হবে।

চলুন দেখি React-এ কীভাবে object পরিবর্তন করা যায়:

### ভুল উপায়:

```jsx
javascript
Copy code
import { useState } from "react";

export default function UserInfo() {
  const [user, setUser] = useState({
    name: "devripon",
    age: 1,
  });

  const handleClick = () => {
    user.age += 1; //👉 এইখানে আপনি সরাসরি object-কে পরিবর্তন করছেন।
    setUser(user);
  };
}

```

### সঠিক উপায়:

```jsx
javascript
Copy code
const handleClick = () => {
  setUser((prevUser) => {
    return {
      name: prevUser.name,
      age: prevUser.age + 1, // এইখানে আপনি object-কে সরাসরি পরিবর্তন না করে সম্পূর্ণ নতুন object দিচ্ছেন।
    };
  });
};

```

### আপনি Local Mutation করতে পারেন:

```jsx
javascript
Copy code
const handleClick = () => {
  // Local mutation
  const updatedUser = user;
  updatedUser.age += 1; // Local variable-কে পরিবর্তন করা হচ্ছে
  setUser(updatedUser); // Mutated object দিয়ে state আপডেট করা হচ্ছে
};

```

### কপি (Spread Operator) করার মাধ্যমে object পরিবর্তন করতে পারেন:

```jsx
javascript
Copy code
const handleClick = () => {
  setUser({
    ...user, // আগের সবকিছু কপি করা হচ্ছে।
    age: user.age + 1, // শুধুমাত্র 'age' প্রোপার্টি আপডেট করা হচ্ছে
  });
};

```

🤚 **বি.দ্র:** কপি (…👉 Spread Operator) করলে শুধুমাত্র এক লেভেল কপি করবে।

### রিঅ্যাক্টে নেস্টেড অবজেক্ট পরিবর্তন: কীভাবে সঠিকভাবে করবেন

চলুন দেখি কীভাবে nested object আপডেট করা যায়:

```jsx
javascript
Copy code
const [person, setPerson] = useState({
  name: 'devripon',
  artwork: {
    title: 'react.js',
    city: 'dhaka',
  }
});

```

```jsx
javascript
Copy code
const updateCity = () => {
  // নতুন updated artwork object তৈরি করা হচ্ছে
  const nextArtwork = { ...person.artwork, city: 'sylhet' };
  // নতুন updated person object তৈরি করা হচ্ছে
  const nextPerson = { ...person, artwork: nextArtwork };
  // person state আপডেট করা হচ্ছে
  setPerson(nextPerson);
};

```

এটি কোডে একটি “নেস্টেড” অবজেক্টের মতো দেখায়:

```jsx
javascript
Copy code
let obj = {
  name: 'devripon',
  artwork: {
    title: 'react.js',
    city: 'dhaka',
  }
};

```

তবে, “নেস্টিং” হল একটি অযাচিত ধারণা কিভাবে অবজেক্টগুলি আচরণ করে। যখন কোডটি কার্যকর হয়, তখন “নেস্টেড” অবজেক্টের মতো কিছুই নেই। আপনি আসলে দুটি ভিন্ন অবজেক্ট দেখছেন:

```jsx
javascript
Copy code
let obj1 = {
  title: 'react.js',
  city: 'dhaka',
};

let obj2 = {
  name: 'devripon',
  artwork: obj1
};

```

obj1 অবজেক্টটি obj2 এর “ভিতরে” নেই। উদাহরণস্বরূপ, obj3-ও obj1-এ “পয়েন্ট” করতে পারে:

```jsx
javascript
Copy code
let obj1 = {
  title: 'react.js',
  city: 'dhaka',
};

let obj2 = {
  name: 'devripon',
  artwork: obj1
};

let obj3 = {
  name: 'javaScript',
  artwork: obj1
};

```

যদি আপনি obj3.artwork.city পরিবর্তন করেন, তবে এটি উভয় obj2.artwork.city এবং obj1.city-কে প্রভাবিত করবে। কারণ obj3.artwork, obj2.artwork এবং obj1 একই অবজেক্ট। অবজেক্টগুলিকে “নেস্টেড” হিসাবে ভাবলে এটি দেখা কঠিন। বরং, এগুলি পৃথক অবজেক্ট যা পরস্পরের দিকে “পয়েন্ট” করছে তাদের প্রপার্টির মাধ্যমে।
